apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-todoapp
  namespace: todoapp
spec:
  volumeName: pv-todoapp
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi


